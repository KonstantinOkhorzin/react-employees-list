import { Header, Title, SubTitle} from './AppInfo.styled';

const AppInfo = () => {
  return (
    <Header>
      <Title>Employees accounting in the company</Title>
      <SubTitle>Total emploees:</SubTitle>
      <SubTitle>Extra payment gets:</SubTitle>
    </Header>
  );
};

export default AppInfo;
